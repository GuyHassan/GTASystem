// history package downloaded automatic from react-router-dom
import { createBrowserHistory } from 'history';
// this is help us to the get the history of our route with function like history.pust()
export default createBrowserHistory();